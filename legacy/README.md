# Legacy menu

The previous homepage is preserved at `/legacy/`, with its original stylesheet
and copies of its artwork. Game links point to the existing games one directory
above. The old, nonexistent `ml-visualized/` destination is now a non-clickable
placeholder.

To retire this fallback, delete this entire directory and remove the `legacy`
link from the root `index.html`. The new menu does not import anything here.
